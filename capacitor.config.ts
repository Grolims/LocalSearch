import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.localsearch.app',
  appName: 'LocalSearch',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
